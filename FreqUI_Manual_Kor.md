# 📈 FreqUI (WebUI) 실전 사용 및 환경 설정 한글 매뉴얼

본 매뉴얼은 퀀트 자동 매매 시스템인 Freqtrade와 머신러닝 모듈(FreqAI)의 작동 상태를 직관적으로 모니터링하고 제어할 수 있는 공식 웹 인터페이스 **`FreqUI`**의 한글 사용 지침서입니다.

---

## 1. 🚀 FreqUI 개요 및 시작 방법

### 1) FreqUI란?
Freqtrade 봇의 REST API와 웹소켓(WebSocket) 기술을 활용해 브라우저에서 실시간으로 봇의 자산 상태, 가동 상태, 진입/청산 로그, 그리고 FreqAI의 차트 분석 화면을 한눈에 관리할 수 있게 돕는 공식 대시보드입니다.

### 2) 가동 방법
바탕화면에 설치된 **`FreqTradeAI`** (차트 모양 아이콘)을 더블 클릭하십시오.
* 봇의 연산 엔진이 가동되는 즉시 백그라운드 포트 감지기가 작동하여 웹 서버(localhost:8080)가 완전히 준비되었을 때 브라우저 탭을 자동으로 열어줍니다.

### 3) 로그인 및 자동화
* **자동 로그인 패치 적용 완료**: 접속 시 ID(`freqtrader`)와 PW(`password`)가 자동으로 양식에 채워지고 로그인 버튼이 0.3초 이내에 자동 격발되므로, 별도의 타이핑 없이 바로 메인 대시보드로 진입하게 됩니다.

---

## 2. 📊 핵심 화면별 주요 기능 및 사용 가이드

FreqUI는 상단 및 좌측의 네비게이션 메뉴를 통해 아래 4가지 핵심 화면으로 구성됩니다.

### ① 대시보드 (Dashboard)
실시간 자산 변화와 현재 가동 상태를 보여주는 메인 컨트롤 센터입니다.
* **상단 요약 지표**:
  - `Profit (누적 손익)`: 봇 가동 시작 이후 실현된 누적 이득/손실 통계입니다.
  - `Balance (계정 잔고)`: Bybit 거래소 선물 지갑의 실시간 자산 현황입니다.
  - `Winrate (승률)`: 봇이 진입하여 익절 청산하고 나온 누적 승리 비율입니다.
* **봇 제어 버튼 (Bot Controller)**:
  - `Start (재개)` / `Stop (정지)`: 봇의 자동 주문 가동을 켜고 끌 수 있습니다.
  - `Pause (일시 중단)`: 신규 포지션 진입을 멈추고, 현재 보유한 포지션만 유지/관리(청산)하도록 전환합니다.
* **활성 거래 목록 (Active Trades)**:
  - 현재 봇이 잡고 있는 롱(LONG) 또는 숏(SHORT) 포지션 정보가 실시간으로 표기됩니다.
  - 진입가, 수량, 현재 미실현 손익(Real-time Profit %)이 지속 갱신됩니다.
  - **비상 탈출 (`Force Exit`)**: 비상 상황 발생 시 이 버튼을 누르면 즉시 거래소 API를 통해 해당 포지션을 **시장가(Market)로 즉시 강제 청산**합니다.

### ② 실시간 차트 (Charts)
FreqAI가 예측하는 데이터와 가격 움직임을 직관적인 캔들로 결합해 표기하는 화면입니다.
* **진입/청산 마커**: 봇이 차트 어디서 진입하고 탈출했는지 캔들 위에 화살표와 레이블로 직관적으로 도식화됩니다.
* **FreqAI 예측 시각화 (`&-target_roi`)**:
  - 차트 하단 혹은 보조 패널에서 FreqAI 모델이 실시간 연산하고 있는 기대 변동성 파형을 볼 수 있습니다. 이 값이 상단 파라미터 임계값을 뚫을 때 진입 마커가 생성되는 인과 관계를 검증할 수 있습니다.

### ③ 실시간 로그 (Logs)
봇의 머신러닝 학습 상황 및 주문 체결 과정을 텍스트로 중계해 주는 곳입니다.
* **FreqAI Retrain (재학습)**: FreqAI가 새로운 캔들을 수집하여 LightGBM 모델을 다시 학습시키는 연산 과정과 예측 스코어가 실시간 갱신됩니다.
* **에러 진단**: Bybit API 서버 연결 지연이나 자금 부족 등의 렉이 생길 경우 빨간색 경고(ERROR/WARNING) 메시지로 즉시 표기되므로, 시스템 이상 진단 시 최우선으로 확인해야 할 화면입니다.

### ④ 거래 잠금 설정 (Locking)
특정 자산(BTC 또는 ETH)에 급격한 비정상 변동성이나 연속 손절이 발생했을 때, 봇의 파산을 방어하기 위해 일시적으로 진입을 잠그는 리스크 필터 관리 장치입니다.
* 봇이 리스크 관리 차원에서 자동으로 거래를 잠근 상태인 경우 이 탭에 페어 정보가 표시됩니다. 필요시 우측의 자물쇠 해제 버튼을 눌러 **수동으로 거래를 즉시 재개**할 수도 있습니다.

---

## 3. 🛠️ 핵심 환경 설정 가이드 (`config.json` 연동)

웹 UI 상단의 `Configuration (환경 설정)` 탭에서는 [config.json](file:///c:/Users/7supp/.gemini/antigravity/260509_freqtrade/config.json) 에 정의된 핵심 리스크 설정값들을 실시간 모니터링할 수 있습니다.

* **최대 오픈 포지션 (`max_open_trades`)**:
  - 현재 **`2`**로 세팅되어 있어, 비트코인과 이더리움을 한 계좌 시드 내에서 동시에 각각 1개씩(총 2개)만 쥘 수 있도록 통제하고 있습니다.
* **진입 및 청산 가격 규칙 (`entry_pricing` / `exit_pricing`)**:
  - 주문의 즉각 체결을 위해 시장가(Market) 진입과 청산을 기본으로 하되, 지정가 미체결 시 10초 단위로 따라가며 체결시키는 안전 보완 스크립트가 백그라운드로 장착되어 있습니다.
* **머신러닝 학습 옵션 (`freqai` 섹션)**:
  - FreqAI의 학습 간격(`train_period_days`) 및 모델 유형(`LightGBMRegressor`)이 정상 설정되어 동작하는지 점검할 수 있습니다.

---

## ⚠️ 4. 안전한 운용을 위한 트러블슈팅 (FAQ)

### Q. 브라우저 대시보드 화면이 멈추거나 먹통이 될 때는 어떻게 하나요?
* **원인**: 봇 엔진 프로세스가 꺼졌거나, Bybit API 통신 장애 등으로 연결이 끊겼을 때 발생합니다.
* **해결**: 실행 중인 FreqTradeAI 터미널 창을 닫고, 바탕화면의 바로가기 아이콘을 다시 더블클릭하여 엔진을 재기동해 주십시오.

### Q. 텔레그램 명령으로도 제어가 가능한가요?
* **네, 가능합니다.** 텔레그램 방에 접속하여 `/status`, `/balance`, `/profit` 을 치시면 언제 어디서나 봇 상태를 폰으로 조회할 수 있습니다.
* 만약 급작스러운 시장 대폭락 등으로 비상 정지를 원하시면 텔레그램 창에 **`/forceexit all`** 혹은 **`/stop`** 을 입력하여 전 포지션을 즉시 시장가 청산하고 대기 상태로 전환할 수 있습니다.

### Q. 실전 가동 중 자금 부족(Insufficient Balance) 경고가 뜹니다.
* **원인**: 거래소에 보관한 USDT 대비 레버리지 및 베팅 비율 설정이 맞지 않거나, 펀딩비 지출로 마진 예치금이 최소 수준 이하로 내려갔을 때 뜹니다.
* **해결**: Bybit 선물 지갑에 예치한 시드머니가 충분한지 점검해 주십시오.

### Q. 차트 화면에서 시간봉(1분, 5분, 1시간 등)을 마음대로 바꿀 수 없나요? 설정이 안 보입니다.
* **원인**: Freqtrade 봇은 메모리 및 Bybit API 호출 한도를 최적화하기 위해, 현재 구동 중인 전략(`V8_BTC_ETH_optimized.py`)과 FreqAI 설정에서 정의된 메인 시간봉(여기서는 **`4시간봉(4h)`**)의 데이터만 거래소로부터 실시간으로 다운로드하고 캐싱합니다. FreqUI 차트는 봇이 직접 수집하여 보관하고 있는 데이터만을 API를 통해 전달받아 시각화하므로, 수집하지 않고 있는 다른 시간봉 데이터는 차트에서 보여줄 수 없어서 선택 메뉴가 보이지 않거나 비활성화됩니다.
* **해결책**: 봇이 바라보는 4시간봉의 진입/청산 마커 외에 15분봉, 1시간봉, 1일봉 등 다양한 시간대에서 상세한 기술적 분석을 하려면, **Bybit 거래소 웹사이트나 트레이딩뷰(TradingView)를 별도의 브라우저 창으로 띄워서 함께 확인하는 것을 권장**합니다. FreqUI 차트는 봇의 동작 상태를 직관적으로 모니터링하는 디버깅용 보조 툴입니다.
